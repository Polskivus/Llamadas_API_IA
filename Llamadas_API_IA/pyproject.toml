[project]
name = "llamadas-api-ia"
version = "0.1.0"
description = "Proyecto para hacer llamadas a la API de "
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ollama>=0.6.2",
    "python-dotenv>=1.2.3",
]

[project.scripts]
llamadas-api-ia = "llamadas_api_ia:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
